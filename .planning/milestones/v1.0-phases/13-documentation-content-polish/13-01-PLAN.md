---
phase: 13-documentation-content-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - CHANGELOG.md
  - examples/basic-setup.md
  - slack-app-manifest.yaml
  - tsconfig.json
autonomous: true
requirements:
  - M21
  - M22
  - M23
  - M24
  - L6
  - L13
  - L14

must_haves:
  truths:
    - "README opening paragraph defines 'MCP server' and 'Socket Mode' before using them"
    - "Placeholder syntax is concrete (a1b2c) in basic-setup.md, not template-style ({id})"
    - "SERVER_NAME config row tells readers where the name appears in Claude's UI"
    - "CHANGELOG.md has same-day dates note, diff link footer, and Breaking Changes audience qualifier"
    - "slack-app-manifest.yaml has workspace-wide scope breadth warning and Socket Mode interactivity note"
    - "tsconfig.json has inline comment explaining skipLibCheck purpose"
  artifacts:
    - path: "README.md"
      provides: "Updated opening paragraph + config table SERVER_NAME row"
      contains: "tool-plugin process"
    - path: "CHANGELOG.md"
      provides: "Dates note + diff link footer + Breaking Changes audience qualifier"
      contains: "same day"
    - path: "examples/basic-setup.md"
      provides: "Concrete permission example with button-first wording"
      contains: "a1b2c"
    - path: "slack-app-manifest.yaml"
      provides: "Scope breadth comment + interactivity Socket Mode comment"
      contains: "workspace-wide"
    - path: "tsconfig.json"
      provides: "skipLibCheck inline explanation comment"
      contains: "skipLibCheck"
  key_links:
    - from: "README.md permission relay section"
      to: "examples/basic-setup.md What's next"
      via: "a1b2c example ID must match across both files"
      pattern: "a1b2c"
---

<objective>
Apply seven targeted documentation polish edits identified in the deep review (2026-03-28). All changes are pure text: two README.md improvements (opening paragraph clarity + config table entry), one CHANGELOG.md addition (dates note + diff links + audience qualifier), one basic-setup.md fix (button-first wording + concrete ID example), two YAML comment additions to slack-app-manifest.yaml, and one JSONC comment in tsconfig.json.

Purpose: Eliminate jargon-before-definition confusion for new readers, fix a placeholder that looks like an unfilled template, add audit-trail metadata to CHANGELOG, and document security surface in the manifest.
Output: Five files edited with ~15 line additions. No code changes. Existing test suite unaffected.
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-documentation-content-polish/13-RESEARCH.md
</context>

<interfaces>
<!-- Current file state for executor reference. No exploration needed. -->

README.md line 8 (to be rewritten — M22):
```
An MCP server that bridges Claude Code sessions to a Slack channel via Socket Mode. Claude receives commands from Slack, replies in threads, and posts permission prompts that operators approve or deny — all from their phone if they want to. No webhooks, no public URLs, no port forwarding.
```

README.md line 172 (to be edited — M23):
```
| `SERVER_NAME` | No | Identifier in Claude's context. Defaults to `slack`. Useful when running multiple instances. |
```

examples/basic-setup.md line 122 (to be replaced — M21):
```
- To approve or deny a tool call from Slack, reply `yes {id}` or `no {id}` in the active thread when a permission request appears.
```

CHANGELOG.md structure (for M24):
- Line 8: `## [Unreleased]` — no diff link
- Lines 21-27: `## [0.2.0] - 2026-03-27 / ### Breaking Changes` — no audience qualifier
- All three versions (0.1.0, 0.2.0, 0.3.0) dated 2026-03-27 with no explanation
- File ends at line 56 with no diff link footer

slack-app-manifest.yaml lines 31-33 (for L6):
```yaml
      - chat:write        # post messages as the bot
      - channels:history  # receive message events from public channels
      - groups:history    # receive message events from private channels
```

slack-app-manifest.yaml lines 40-41 (for L14):
```yaml
  interactivity:
    is_enabled: true      # required for Approve/Deny buttons on permission requests
```

tsconfig.json line 13 (for L13):
```json
    "skipLibCheck": true,
```
Note: TypeScript and Bun both parse tsconfig.json as JSONC — `//` comments are safe even in a .json file.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: README.md — opening paragraph and SERVER_NAME row</name>
  <files>README.md</files>
  <action>
Make two targeted edits to README.md:

**Edit 1 — M22: Opening paragraph (line 8)**
Replace the current single-sentence opening:
```
An MCP server that bridges Claude Code sessions to a Slack channel via Socket Mode. Claude receives commands from Slack, replies in threads, and posts permission prompts that operators approve or deny — all from their phone if they want to. No webhooks, no public URLs, no port forwarding.
```
With parenthetical definitions added inline:
```
An MCP server (a tool-plugin process that Claude Code loads on startup) that bridges Claude Code sessions to a Slack channel via Socket Mode (a WebSocket connection — no public URL required). Claude receives commands from Slack, replies in threads, and posts permission prompts that operators approve or deny — all from their phone if they want to. No webhooks, no public URLs, no port forwarding.
```

**Edit 2 — M23: Config table SERVER_NAME row (line 172)**
Replace:
```
| `SERVER_NAME` | No | Identifier in Claude's context. Defaults to `slack`. Useful when running multiple instances. |
```
With:
```
| `SERVER_NAME` | No | Appears as the MCP server name in Claude's tool list. Defaults to `slack`. Useful when running multiple instances. |
```

Do NOT change any other part of README.md. These are surgical line edits only.
  </action>
  <verify>
    <automated>grep -c "tool-plugin process" /Users/seth/Documents/GitHub/claude-slack-channel/README.md && grep -c "tool list" /Users/seth/Documents/GitHub/claude-slack-channel/README.md</automated>
  </verify>
  <done>README.md opening paragraph contains parenthetical definitions for "MCP server" and "Socket Mode"; SERVER_NAME row leads with "Appears as the MCP server name in Claude's tool list."</done>
</task>

<task type="auto">
  <name>Task 2: CHANGELOG.md — dates note, diff link footer, Breaking Changes qualifier</name>
  <files>CHANGELOG.md</files>
  <action>
Make three targeted additions to CHANGELOG.md (M24):

**Addition 1 — Same-day dates note**
After the two preamble lines (format + semver links, lines 5-6), add a blank line and this note:
```
> These versions were developed and released on the same day during initial build. Dates reflect the initial publish date.
```
Place it between the semver line and the `## [Unreleased]` heading so it reads as a one-time context note for the file.

**Addition 2 — Breaking Changes audience qualifier in 0.2.0**
In the `## [0.2.0]` section, the `### Breaking Changes` subsection currently starts with bullet items. Add one sentence of context at the top of that subsection (before the first bullet):
```
These changes affect library consumers only (code that imports from `claude-slack-channel` directly). CLI users running via `bunx` are unaffected.
```

**Addition 3 — Diff link footer**
At the very end of the file (after the last line of `## [0.1.0]`), add a blank line and these reference-style diff links:
```
[Unreleased]: https://github.com/sethbrasile/claude-slack-channel/compare/v0.3.0...HEAD
[0.3.0]: https://github.com/sethbrasile/claude-slack-channel/compare/v0.2.0...v0.3.0
[0.2.0]: https://github.com/sethbrasile/claude-slack-channel/compare/v0.1.0...v0.2.0
[0.1.0]: https://github.com/sethbrasile/claude-slack-channel/releases/tag/v0.1.0
```

Do NOT change any existing CHANGELOG content — these are additive insertions only.
  </action>
  <verify>
    <automated>grep -c "same day" /Users/seth/Documents/GitHub/claude-slack-channel/CHANGELOG.md && grep -c "library consumers" /Users/seth/Documents/GitHub/claude-slack-channel/CHANGELOG.md && grep -c "compare/v0.3.0" /Users/seth/Documents/GitHub/claude-slack-channel/CHANGELOG.md</automated>
  </verify>
  <done>CHANGELOG.md has a same-day dates note after the preamble, a library-consumers qualifier at the top of the 0.2.0 Breaking Changes section, and a diff link footer at end of file with four reference-style links.</done>
</task>

<task type="auto">
  <name>Task 3: basic-setup.md, slack-app-manifest.yaml, tsconfig.json — remaining polish</name>
  <files>examples/basic-setup.md, slack-app-manifest.yaml, tsconfig.json</files>
  <action>
Make four targeted edits across three files:

**Edit 1 — M21: examples/basic-setup.md "What's next" (line 122)**
Replace:
```
- To approve or deny a tool call from Slack, reply `yes {id}` or `no {id}` in the active thread when a permission request appears.
```
With (button-first wording, concrete ID example):
```
- When a permission request appears, click **Approve** or **Deny** in-thread. Or reply `yes a1b2c` / `no a1b2c` (replace `a1b2c` with the request ID shown in the prompt) as a text fallback.
```
Do not touch any other part of the file.

**Edit 2 — L6: slack-app-manifest.yaml scope breadth comment**
Before the `- channels:history` line (currently line 32), insert a four-line block comment:
```yaml
      # NOTE: channels:history and groups:history grant workspace-wide read access to
      # message events in any channel the bot joins — not scoped to SLACK_CHANNEL_ID alone.
      # The server filters by SLACK_CHANNEL_ID in code. Review before granting to
      # shared workspaces with sensitive channels.
```

**Edit 3 — L14: slack-app-manifest.yaml interactivity comment**
Replace the current single-line interactivity comment:
```yaml
    is_enabled: true      # required for Approve/Deny buttons on permission requests
```
With an extended comment (same value, additional note):
```yaml
    is_enabled: true      # required for Approve/Deny buttons; also required for Socket Mode
                          # to receive interactive payload events (button clicks)
```

**Edit 4 — L13: tsconfig.json skipLibCheck comment**
In tsconfig.json, the file is parsed as JSONC by TypeScript and Bun — `//` comments are safe despite the .json extension. Add a two-line comment immediately above the `"skipLibCheck": true` line:
```jsonc
    // suppresses type errors in .d.ts files from dependencies; needed because some
    // third-party type declarations are incompatible with strict mode settings
    "skipLibCheck": true,
```
  </action>
  <verify>
    <automated>grep -c "a1b2c" /Users/seth/Documents/GitHub/claude-slack-channel/examples/basic-setup.md && grep -c "workspace-wide" /Users/seth/Documents/GitHub/claude-slack-channel/slack-app-manifest.yaml && grep -c "Socket Mode" /Users/seth/Documents/GitHub/claude-slack-channel/slack-app-manifest.yaml && grep -c "third-party" /Users/seth/Documents/GitHub/claude-slack-channel/tsconfig.json</automated>
  </verify>
  <done>basic-setup.md uses "a1b2c" and leads with Approve/Deny buttons; slack-app-manifest.yaml has workspace-wide scope warning before channels:history and Socket Mode note in interactivity section; tsconfig.json has JSONC comment above skipLibCheck.</done>
</task>

</tasks>

<verification>
After all tasks complete, run the full toolchain to confirm no collateral damage from the text edits:

```bash
cd /Users/seth/Documents/GitHub/claude-slack-channel && bun test && bunx tsc --noEmit && bunx biome check .
```

All three must exit 0. Since no TypeScript source files were touched, failures here would indicate an accidental edit to a code file — investigate immediately.

Manual spot-check:
- Read README.md lines 1-12: both parenthetical definitions present
- Read CHANGELOG.md: diff links visible at end of file, qualifier present before 0.2.0 bullet list
- Confirm `{id}` does not appear in examples/basic-setup.md
</verification>

<success_criteria>
- `grep "tool-plugin process" README.md` exits 0
- `grep "tool list" README.md` exits 0
- `grep "same day" CHANGELOG.md` exits 0
- `grep "library consumers" CHANGELOG.md` exits 0
- `grep "compare/v0.3.0" CHANGELOG.md` exits 0
- `grep "a1b2c" examples/basic-setup.md` exits 0
- `grep -c "{id}" examples/basic-setup.md` returns 0
- `grep "workspace-wide" slack-app-manifest.yaml` exits 0
- `grep "Socket Mode" slack-app-manifest.yaml` exits 0
- `grep "third-party" tsconfig.json` exits 0
- `bun test` passes (no code changed)
- `bunx tsc --noEmit` exits 0
- `bunx biome check .` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/13-documentation-content-polish/13-01-SUMMARY.md` using the summary template at `@/Users/seth/.claude/get-shit-done/templates/summary.md`.
</output>
