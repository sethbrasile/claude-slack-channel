---
phase: 12-documentation-setup-flow-consistency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - H5
  - M16
  - M18
  - M19
  - M20
  - M25
  - L21
  - L23

must_haves:
  truths:
    - "A Prerequisites section appears before the Quick Start heading listing Claude Code v2.1.80+, claude.ai login requirement, Bun, and Slack admin permissions"
    - "The inline Bun block quote that currently sits under Quick Start is removed (content is in Prerequisites now)"
    - "The Channel ID row in the configuration table shows the URL format inline"
    - "The ALLOWED_USER_IDS row documents both U0XXXXXXXXX and W0XXXXXXXXX formats"
    - "Step 7 says /invite @Claude (the display name set in the manifest) not @YourBotName"
    - "Step 6 audit paragraph includes the clarification about where to run Claude Code"
    - "The Examples section appears before the Comparison section in the README"
    - "A Troubleshooting section with 4-6 common issues exists after Examples and before Development"
  artifacts:
    - path: "README.md"
      provides: "Updated README with all Phase 12 documentation fixes"
      contains: "Prerequisites"
  key_links:
    - from: "README.md Prerequisites"
      to: "Quick start"
      via: "Section order — Prerequisites appears immediately before Quick start"
    - from: "README.md Examples"
      to: "Comparison section"
      via: "Section order — Examples appears before Comparison"
---

<objective>
Apply all Phase 12 README.md documentation fixes: add a Prerequisites section (H5, M16), improve inline
documentation for Channel ID URL format (M18), W0 user ID format (M19), bot invite command (M20), audit
step clarity (M25), reposition the Examples section above Comparison (L21), and add a Troubleshooting
section (L23).

Purpose: Reduce setup friction and surface security-relevant prerequisites before operators begin following the Quick Start steps.
Output: Updated README.md with all 8 findings addressed.
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-documentation-setup-flow-consistency/12-RESEARCH.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Prerequisites section and remove inline Bun block quote</name>
  <files>README.md</files>
  <action>
Insert a new `## Prerequisites` section immediately before `## Quick start` (currently line 35). The section content:

```markdown
## Prerequisites

- **[Claude Code](https://docs.anthropic.com/en/docs/claude-code) v2.1.80+** with a [claude.ai](https://claude.ai) login. An API key alone is not sufficient — the Channel protocol requires claude.ai authentication.
- **[Bun](https://bun.sh)** installed. Use `bunx` to run. `npx` will not work — the entry point is TypeScript executed directly by Bun.
- **Slack workspace admin or app-management permissions** to create and install a Slack app.

---
```

After inserting the Prerequisites section, delete the existing block quote that currently appears as the first line under `## Quick start`:
```
> **Requires [Bun](https://bun.sh).** Use `bunx` to run. `npx` will not work — the entry point is TypeScript executed directly by Bun.
```

This removes duplicate content — the Bun note is now in Prerequisites. Do NOT remove the block quote at line 114 that says "Requires Claude Code v2.1.80+..." — that note stays in place at Step 7 until it is superseded. Actually remove it too since Prerequisites covers it: the `> Requires Claude Code v2.1.80+` block quote at the end of Step 7 (currently line 114, after the code block). Both block quotes should be removed — their content now lives in Prerequisites.
  </action>
  <verify>
    <automated>grep -c "## Prerequisites" README.md && ! grep -q "Requires \[Bun\]" README.md && echo "block quote removed"</automated>
  </verify>
  <done>Prerequisites section exists before Quick start; both old block quotes are gone; no content is lost — Bun note, claude.ai login, and Slack admin requirement are all present in Prerequisites.</done>
</task>

<task type="auto">
  <name>Task 2: Fix config table, bot invite, audit step clarity</name>
  <files>README.md</files>
  <action>
Make four targeted edits to README.md:

**M18 — Channel ID URL format in Quick Start Step 4 table:**
Find the row:
```
| Channel ID (`C0...`) | Right-click the channel in Slack > **Copy link** > last path segment of the URL |
```
Replace with:
```
| Channel ID (`C0...`) | Right-click the channel in Slack > **Copy link** > last path segment of the URL (e.g. `https://yourworkspace.slack.com/archives/C0XXXXXXXXX`) |
```

**M19 — W0 user ID format in Configuration table:**
Find the row:
```
| `ALLOWED_USER_IDS` | Yes | Comma-separated Slack user IDs allowed to send commands |
```
Replace with:
```
| `ALLOWED_USER_IDS` | Yes | Comma-separated Slack user IDs allowed to send commands. Format: `U0XXXXXXXXX` (regular) or `W0XXXXXXXXX` (workspace accounts) |
```

**M20 — Bot invite command in Step 7:**
Find:
```bash
# In Slack: /invite @YourBotName
```
Replace with:
```bash
# In Slack: /invite @Claude  (the display name set in the manifest)
```

**M25 — Audit step clarification in Step 6:**
Find the paragraph that begins "Clone the repo at the tag you're about to run:" and add the clarification. The introductory paragraph for Step 6 currently ends with "You should repeat this audit every time you bump the pinned version." Append to that sentence so it reads:

"The `--dangerously-load-development-channels` flag in the next step gives this server the ability to relay permission prompts and approval responses to Claude Code. Before you trust it with that, verify the code is safe. **You should repeat this audit every time you bump the pinned version.**"

Add a new sentence after this paragraph:

"(Run this audit in any terminal where Claude Code is available.)"

This goes on its own line, before the code block that follows.
  </action>
  <verify>
    <automated>grep -c "W0XXXXXXXXX" README.md && grep -c "archives/C0XXXXXXXXX" README.md && grep -c "@Claude" README.md && grep -c "any terminal where Claude Code is available" README.md</automated>
  </verify>
  <done>All four targeted edits applied: URL format shown inline in Step 4 table, W0 format documented in Configuration table, bot invite uses @Claude with note, audit step has clarifying parenthetical.</done>
</task>

<task type="auto">
  <name>Task 3: Reposition Examples section and add Troubleshooting</name>
  <files>README.md</files>
  <action>
**L21 — Move Examples section before Comparison:**

Current order in README.md (after Configuration and How it works sections):
1. `## Comparison with community implementation` (~line 207)
2. `## Examples` (~line 222)
3. `## Development` (~line 230)

Cut the entire `## Examples` section (two lines plus the `---` separator after it) and paste it immediately before `## Comparison with community implementation`. Target order:
1. `## Examples`
2. `## Comparison with community implementation`
3. (new) `## Troubleshooting`
4. `## Development`

**L23 — Add Troubleshooting section after Comparison:**

Insert the following section between `## Comparison with community implementation` (and its trailing `---`) and `## Development`:

```markdown
## Troubleshooting

| Symptom | Likely cause | Fix |
|---------|-------------|-----|
| Claude doesn't respond to messages | Bot not invited to channel, or wrong `SLACK_CHANNEL_ID` | Run `/invite @Claude` in the channel; verify the channel ID matches the `C0...` segment in the URL |
| Server starts but disconnects immediately | Invalid `SLACK_APP_TOKEN` (must start with `xapp-`) or `connections:write` scope missing | Check token prefix; verify the scope was added when generating the app-level token under **Basic Information > App-Level Tokens** |
| "Invalid config" on startup | Missing or malformed env vars | Check all four required vars are set; user IDs must start with `U` or `W` |
| Permission buttons appear but clicking does nothing | Interactivity not enabled in the Slack app | Verify interactivity is enabled in Slack app settings (the manifest enables it by default) |
| Works locally but fails on a remote VM | Firewall blocking outbound WebSocket | Socket Mode uses port 443 outbound only — confirm outbound HTTPS is allowed |

---
```
  </action>
  <verify>
    <automated>grep -n "## Examples\|## Comparison\|## Troubleshooting\|## Development" README.md</automated>
  </verify>
  <done>README.md section order is: Examples, then Comparison, then Troubleshooting, then Development. Troubleshooting table has 4-6 entries covering common first-time setup failures.</done>
</task>

</tasks>

<verification>
After all three tasks:

```bash
# Confirm Prerequisites section exists
grep -c "## Prerequisites" README.md

# Confirm old Bun block quote removed
grep -c "Requires \[Bun\]" README.md   # must return 0

# Confirm version-specific improvements
grep -c "W0XXXXXXXXX" README.md
grep -c "archives/C0XXXXXXXXX" README.md
grep -c "@Claude" README.md
grep -c "any terminal where Claude Code is available" README.md

# Confirm section ordering (Examples before Comparison)
grep -n "## Examples\|## Comparison\|## Troubleshooting\|## Development" README.md

# Confirm Troubleshooting section
grep -c "## Troubleshooting" README.md

# Confirm tests still pass (no source files touched)
bun test
```
</verification>

<success_criteria>
- Prerequisites section appears before Quick Start covering: Claude Code v2.1.80+, claude.ai login, Bun, Slack admin permissions
- Old inline Bun block quote under Quick Start is removed (no duplication)
- Channel ID row in Step 4 table shows full URL format example inline
- ALLOWED_USER_IDS row in Configuration table documents W0XXXXXXXXX format
- Step 7 bot invite uses `/invite @Claude (the display name set in the manifest)` not `@YourBotName`
- Step 6 audit section has clarifying parenthetical about where to run Claude Code
- Examples section appears before Comparison section in document
- Troubleshooting section with 4-6 rows exists after Comparison and before Development
- `bun test` passes (no source files modified)
</success_criteria>

<output>
After completion, create `.planning/phases/12-documentation-setup-flow-consistency/12-01-SUMMARY.md`
</output>
